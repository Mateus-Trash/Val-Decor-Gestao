# Disable build script verification for deployment environments
# This allows pnpm to install dependencies without requiring explicit approval
# for build scripts, which is necessary for CI/CD and hosting platforms
ignore-scripts=false
